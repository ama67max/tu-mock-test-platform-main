# Specification: frontend/src/pages/AdminAnalytics.jsx

## Purpose

Admin analytics page composed of `PerformanceChart` and `SubjectBreakdown` and other admin stats; uses `analyticsApi`.

## Behavior

Fetch analytics for selected exam and render charts.
