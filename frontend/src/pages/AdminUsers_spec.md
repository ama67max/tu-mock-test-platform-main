# Specification: frontend/src/pages/AdminUsers.jsx

## Purpose

Admin users management page. Uses `adminApi.getUsers`, `adminApi.updateUser`, and `adminApi.deleteUser`.

## Behavior

- Fetch paginated users, display `UserTable` with actions wired.
