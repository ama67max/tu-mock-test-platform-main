# README.md Spec (Task 115)

Purpose:

- Provide a clear project README that explains how to set up the development environment, run services with Docker Compose, and a basic deployment guide.

Requirements:

- Short project description.
- Prerequisites (Docker, Node, npm/yarn).
- Local development: how to run backend and frontend locally and using Docker Compose.
- Environment variables and where to put them.
- How to run tests for frontend (Vitest) and backend (jest/mocha if present).
- Commands for building images and running in production behind `nginx`.
- Links to useful files: `docker-compose.yml`, `nginx/nginx.conf`, `Makefile`, `高并发试题平台.md`.

Notes:

- Keep it concise but practical for contributors.
